# Site settings
title: Bucardo

# Build settings
markdown: kramdown

baseurl: ""

defaults:
    -
        scope:
            path: "" # Everything
        values:
            layout: "default"
            project: "bucardo"
    -
        scope:
            path: "check_postgres"
        values:
            project: "check_postgres"
    -
        scope:
            path: "Dbixsafe"
        values:
            project: "dbix_safe"
    -
        scope:
            path: "tail_n_mail"
        values:
            project: "tail_n_mail"
    -
        scope:
            path: "Boxinfo"
        values:
            project: "boxinfo"

gems:
    - jekyll-redirect-from

exclude: ['README', 'Gemfile', 'Gemfile.lock']
